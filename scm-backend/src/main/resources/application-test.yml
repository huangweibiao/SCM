server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/scm_db_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: INFO
    com.scm: DEBUG
    org.springframework.security: DEBUG
